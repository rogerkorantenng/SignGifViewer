commands:
  01_node_install:
    command: "source /tmp/.nvm/nvm.sh && nvm install 20 && nvm use 20"
    ignoreErrors: true

container_commands:
  01_clean_modules:
    command: "rm -rf /var/app/staging/node_modules"
  02_npm_install:
    command: "npm install"
    cwd: "/var/app/staging"
